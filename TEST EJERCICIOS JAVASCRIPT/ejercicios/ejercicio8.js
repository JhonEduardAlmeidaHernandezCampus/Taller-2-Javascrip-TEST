export const perimetro = (lado) => {
    let totalPerimetro = 4 * lado

    return `El perimetro del cuadrado es de ${totalPerimetro}`
}

export const area = (base, altura) => {
    let totalArea = base * altura
    
    return `El area del rectangulo es de ${totalArea}`
}